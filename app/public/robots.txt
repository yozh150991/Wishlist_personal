# Заборона рівно одна, і вона має сенс: у гостьовому посиланні секрет — сама
# адреса (CLAUDE.md §3.5). Решта сторінок за автентифікацією, сканеру там
# видно лише форму входу, тож забороняти їх — зайвий шум.
#
# robots.txt лише просить не сканувати. Від потрапляння в індекс гостьові
# сторінки захищає заголовок X-Robots-Tag у vercel.json; `npm run check:pwa`
# стежить за обома.
User-agent: *
Disallow: /s/
