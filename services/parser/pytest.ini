[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# Корінь сервісу в sys.path: інакше `pytest` (не `python -m pytest`) не знаходить пакет app.
pythonpath = .
